# ربط وكيلك كنواة (Brain) لأي واجهة JARVIS

وكيلك يعمل الآن كـ **خادم نواة عالمي**: أي واجهة JARVIS — بما فيها
[jarvis.iprog.dev](https://jarvis.iprog.dev/) إن كانت تتيح ضبط عنوان backend —
تتصل به فتصير أنت العقل خلفها.

> ملاحظة صادقة: بيئة السحابة هنا محجوب فيها `jarvis.iprog.dev` (سياسة الشبكة)،
> فلم أستطع قراءة صيغة طلباتها بالضبط. لذلك جعلت الخادم **يقبل كل الصيغ الشائعة
> ويردّ بكلها** — يعمل مع الأغلب دون تخصيص. إن كانت واجهتها تستخدم صيغة غريبة،
> أعطني مثال طلب/رد منها (أو رابط مستودعها) وأضبطه لها بالضبط.

## 1) شغّل النواة على جهازك
```bash
python -m agent_os.server        # يعمل على http://127.0.0.1:8770
```

## 2) وجّه واجهة JARVIS إلى النواة
في إعدادات البرنامج/الموقع، اضبط «Backend / API URL» إلى:
```
http://127.0.0.1:8770/chat
```

## واجهة الـ API (مرنة — تقبل أي صيغة)
`POST /chat`  (أو `/api/chat` أو `/api/ask`) — CORS مفعّل لأي أصل.

يقبل الأمر بأي من هذه المفاتيح في JSON أو query:
`q · message · text · prompt · input · query · command · msg`

ويردّ بكل هذه المفاتيح (خذ ما يناسب واجهتك):
`reply · response · text · message · answer · output · result` (+ `kind`)

مثال:
```bash
curl -X POST http://127.0.0.1:8770/chat \
  -H 'Content-Type: application/json' \
  -d '{"message":"سجّل هدف بناء متجر"}'
# → {"reply":"...","response":"...","answer":"...","kind":"task", ...}
```

`GET /api/status` → جاهزية النظام (عقل/صوت/ذاكرة).

## أو استخدم وجه JARVIS الجاهز (بلا أي تثبيت)
بنيتُ لك واجهة كاملة تعمل في المتصفح:
```bash
python -m agent_os.server   # ثم افتح http://127.0.0.1:8770 في Chrome
```
وجه متوهّج يستيقظ عند قول «جارفيس»، يسمع أمرك (صوت المتصفح)، ينفّذه عبر
النواة، ويردّ بصوته — عربي وإنجليزي، بلا تثبيت faster-whisper أو piper.
