# سجل الإصلاحات المطبّقة — مراجعة Claude

هذا الملف يوثّق كل عيب أُصلح في هذه الجولة. كل الاختبارات تنجح:
**107 passed, 1 skipped** (كانت 102؛ أُضيفت 5 اختبارات أمان جديدة).

## 🔴 عيوب حرجة

### 1. ثغرة تنفيذ أوامر عشوائية (الأخطر)
- **الملف:** `computer_control.py`, `selfrunner.py`
- **المشكلة:** `run_command` كان يستخدم `subprocess.run(cmd, shell=True)` بلا أي
  تحقق. أداة `CONTROL:` في `selfrunner` تمرّر مباشرة إلى `execute_action` →
  `run_command`، مما يلتف على كل حماية `_is_safe_command`. أي حقن تعليمات من
  الويب كان يقدر ينفّذ أوامر كاملة على الجهاز.
- **الإصلاح:**
  - `run_command` الآن يمرّ بـ whitelist نفسه ويُنفّذ عبر `argv` بلا shell.
  - `write_file/read_file/delete_file/copy_file` تتحقق من المسار (تمنع `.env`
    ومجلدات النظام).
  - `open_app` يقتصر على أسماء برامج معروفة، بلا shell.
  - طبقة دفاع ثانية في `selfrunner`: الأفعال الحساسة (run_command/delete_file/
    kill_process/write_file) تتطلب موافقة صريحة عند `AUTO_RUN=ask`.

### 2. خطأ صياغي يعطّل ملفاً كاملاً
- **الملف:** `self_improver.py` سطر 119
- **المشكلة:** `f'{body_indent}"""{دالة {func_name}."""'` — كلمة عربية داخل قوس
  f-string ⇒ `SyntaxError`. الملف لا يُستورد، و`daily_autopilot` يعتمد عليه.
- **الإصلاح:** `f'{body_indent}"""دالة {func_name}."""'`

## 🟠 عيوب متوسطة

### 3. التحسين الذاتي يلتزم على الكود الحي بلا تشغيل اختبارات
- **الملف:** `agent_os/self_improve_engine.py`
- **المشكلة:** دالة `_run_tests_in()` معرّفة لكن **غير مستدعاة أبداً**. تعديل
  يكتبه نموذج LLM كان يُلتزم بمجرد أنه "حمّل بلا خطأ".
- **الإصلاح:** أُضيفت بوابة اختبارات إلزامية (خطوة 4.2) — يجب أن ينجح pytest
  كاملاً على الفرع المعزول قبل أي التزام.

### 4. shell=True
- لم يبقَ أي استخدام فعلي (الباقي مجرد نصوص وصفية وregex في linter).

## 🟡 تحسينات

- **حذف ملفات .bak** الثلاثة + إضافة `*.bak` لـ`.gitignore`.
- **كشف حقن التعليمات:** كان إنجليزياً فقط؛ أُضيفت أنماط عربية + تسوية ضد التهرّب
  (فراغات زائدة/تطويل/محارف صفرية).
- **إصلاح flaky test:** أسماء تقارير `run_task` كانت بدقة الثانية فتتصادم؛ أُضيفت
  لاحقة عشوائية. الآن 107 ينجح على تشغيلات متتالية.
- **5 اختبارات أمان جديدة** تحمي الطبقة الأمنية الجديدة من الانحدار.
