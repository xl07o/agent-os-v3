# مراجعة كاملة للجولة التالية — موظف الليل (SelfRunner) v2.3

أنت تفحص برنامج وكيل ذكاء اصطناعي ذاتي على ويندوز مكتوب بـ Python 3.14، كله بالعربية.
نسخة v2.3 أصلحت بالفعل المراجعة الشاملة السابقة (40 ملاحظة) بنجاح — لا تعِد بتكرارها.

## مكان الكود
- المشروع: `C:\Users\hhdjj\OneDrive\سطح المكتب\كود_الموظف\`
- الأرشيف الجاهز للمراجعة: `C:\Users\hhdjj\OneDrive\سطح المكتب\كود_الموظف_REVIEW.zip`
  (33 ملفاً: المصدر كاملاً + الاختبارات + المستندات — بلا `.env`/سجلات/ذاكرة)

## القراءة الموصى بها أولاً
- `NOTES_FOR_REVIEWER.md` — التاريخ الكامل للإصلاحات (الجولات 1-4) والقرارات الهندسية.
- `tests/test_selfrunner.py` — 55 اختباراً (نجحت مرتين) + 1 شبكي اختياري.

## ما أُصلح في الجولة 4 (لا تِطرحه ثانية)
- حقن أوامر الـ batch في `start.bat` (اقتباسات + !var!)
- حظر `$[`, `<(`, `$((` والاقتباس المكسور في `selfrunner._split_args`
- مجلدات النظام على كل الأقراص + فلترة الروابط الرمزية في `list_project_files`
- أقفال rate/cache وقراءة محدودة 5MB وفحص Content-Type في `webtools`
- جلسة عقل واحدة متصلة في `chat_cli` والذاكرة تُحقن بسياق المستخدم
- ميزانية تكلفة يومية تُصفَّر تلقائياً بعد منتصف الليل + مهلة `as_completed`
  مُوافقة لإعدادات البرنامج
- كتابة ذرية لبنك الذاكرة والـ fingerprints بصمة SHA-256
- تعقيم أسماء نماذج Ollama وأسماء خرائط Roblox (Lua) ضد الحقن
- حد مهمة سخي قابل للضبط + مهلة API قابلة للضبط + `pip --user` احتياطي
- روابط لوحة العرض `rel="noopener noreferrer"` ومسارات نسبية في تقرير الإتقان

## قرارات المستخدم الصريحة — لا تعترض عليها ولا تطلب تغييرها
- السجلات تنمو بلا حدّ (لا تدوير ولا حجم أقصى) — هذا سلوك مقصود.
- المفتاحية تُدار بواسطة `.env` وتستقر في المشروع الحي — لا تفصلها ولا تقيّدها.
- العقل المتعدد: يستخدم `gemini` (مجاناً لا يُحتسب) و`groq` مجاناً — قيد التكلفة
  يخص المزودات المدفوعة فقط.

## المطلوب منك هذه الجولة
- راجع سطراً بسطر: `selfrunner.py`, `brain.py`, `webtools.py`, `skills.py`,
  `mastery.py`, `schedule.py`, `builders.py`, `orchestrator.py`, `autopilot.py`.
- ابحث عن: ثغرات أمنية فعلية، أعطال، سلوك خاطئ، مجالات غير مغطاة في الاختبارات.
- لا تعِد بملاحظات شكلية فقط — كل ملاحظة يجب أن تكون قابلة للتنفيذ ومحدّدة المسار/السطر.
- رقّم الملاحظات وعنونها بخطورتها (حرج/متوسط/تحسين).

## بعد التقييم
- إن وجدت إصلاحات، ستُطبّق مباشرة على الكود وتُعاد الاختبارات والتوثيق والأرشيف.